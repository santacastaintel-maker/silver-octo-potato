# 🏗️ ARQUITECTURA DEL PORTAFOLIO

```
┌─────────────────────────────────────────────────────────────┐
│                     PORTAFOLIO CHA CASTAÑEDA                 │
│              (Arte Urbano + Desarrollo Web)                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │  Homepage   │  │  Projects   │  │    Admin    │        │
│  │  (Periódico)│  │  (Gallery)  │  │    Panel    │        │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘        │
│         │                 │                 │               │
│         └─────────────────┼─────────────────┘               │
│                           │                                  │
│                  ┌────────▼────────┐                        │
│                  │  Custom Hooks   │                        │
│                  │  - useProjects  │                        │
│                  │  - useContact   │                        │
│                  └────────┬────────┘                        │
│                           │                                  │
│                  ┌────────▼────────┐                        │
│                  │  Supabase       │                        │
│                  │  Client         │                        │
│                  └────────┬────────┘                        │
└───────────────────────────┼──────────────────────────────────┘
                            │
                  ┌─────────▼─────────┐
                  │   SUPABASE (BaaS) │
                  ├───────────────────┤
                  │                   │
                  │  📊 PostgreSQL    │
                  │  ├─ projects      │
                  │  ├─ categories    │
                  │  ├─ technologies  │
                  │  └─ contacts      │
                  │                   │
                  │  🔐 RLS + Auth    │
                  │  📦 Storage       │
                  └───────────────────┘

═══════════════════════════════════════════════════════════════

📁 ESTRUCTURA DE ARCHIVOS:

portafolio-cha/
│
├── 📄 index.html ──────────────► SEO + Google Fonts
│
├── src/
│   ├── 🎨 index.css ───────────► Sistema de diseño global
│   ├── 📱 App.jsx ─────────────► Componente raíz
│   │
│   ├── pages/
│   │   ├── Homepage.jsx ───────► Página principal (✅ HECHO)
│   │   ├── ProjectsGallery.jsx ► Galería (⏳ TODO)
│   │   ├── AdminPanel.jsx ─────► Panel CRUD (⏳ TODO)
│   │   └── Contact.jsx ────────► Contacto (⏳ TODO)
│   │
│   ├── components/
│   │   ├── Notification.jsx ───► Sistema de alertas (✅ HECHO)
│   │   ├── ProjectCard.jsx ────► Tarjeta de proyecto (⏳ TODO)
│   │   └── ContactForm.jsx ────► Formulario (⏳ TODO)
│   │
│   ├── hooks/
│   │   ├── useProjects.js ─────► CRUD proyectos (✅ HECHO)
│   │   ├── useCategories.js ───► Categorías (✅ HECHO)
│   │   └── useContactForm.js ──► Contacto (✅ HECHO)
│   │
│   ├── lib/
│   │   └── supabaseClient.js ──► Cliente configurado (✅ HECHO)
│   │
│   ├── styles/
│   │   ├── Homepage.css ───────► Estilos homepage (✅ HECHO)
│   │   ├── ProjectsGallery.css ► Galería (⏳ TODO)
│   │   └── AdminPanel.css ─────► Admin (⏳ TODO)
│   │
│   └── utils/
│       └── (helpers futuros)
│
├── 📊 SUPABASE_SCHEMA.sql ─────► Script de BD (✅ HECHO)
├── 📖 README.md ────────────────► Documentación (✅ HECHO)
├── 🔧 SETUP_SUPABASE.md ────────► Guía setup (✅ HECHO)
└── 📋 RESUMEN_CONSTRUCCION.md ──► Este archivo

═══════════════════════════════════════════════════════════════

🎨 SISTEMA DE DISEÑO (CSS PURO):

┌────────────────────────────────────────────┐
│          VARIABLES CSS (Tokens)            │
├────────────────────────────────────────────┤
│ Colores:                                   │
│  --color-primary: #D32F2F                  │
│  --color-bg-light: #F2E8D5                 │
│  --color-ink: #121212                      │
│                                             │
│ Tipografía:                                │
│  --font-display: Anton                     │
│  --font-headline: Bebas Neue               │
│  --font-body: Playfair Display             │
│  --font-mono: Roboto Mono                  │
│                                             │
│ Espaciado:                                 │
│  --spacing-sm → --spacing-2xl              │
│                                             │
│ Efectos:                                   │
│  --shadow-retro: 4px 4px 0 #000            │
└────────────────────────────────────────────┘
              ▼
┌────────────────────────────────────────────┐
│         CSS Grid (12 columnas)             │
│  .constructivist-grid { display: grid }    │
│  .col-span-{1-12} para distribución        │
└────────────────────────────────────────────┘
              ▼
┌────────────────────────────────────────────┐
│          Componentes Visuales              │
│  • Homepage (periódico constructivista)    │
│  • Notification (alertas personalizadas)   │
│  • Responsive 100%                         │
└────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

🔄 FLUJO DE DATOS:

1. Usuario visita Homepage
   ↓
2. Componente se monta, ejecuta useEffect
   ↓
3. Hook useProjects llama a Supabase
   ↓
4. Supabase retorna proyectos desde PostgreSQL
   ↓
5. Hook actualiza state de React
   ↓
6. Componente re-renderiza con datos
   ↓
7. Usuario ve proyectos en pantalla

═══════════════════════════════════════════════════════════════

🔐 SEGURIDAD:

┌────────────────────────────────────────────┐
│         Variables de Entorno               │
│  .env (Git ignored)                        │
│  ├─ VITE_SUPABASE_URL                      │
│  └─ VITE_SUPABASE_ANON_KEY                 │
└────────────────────────────────────────────┘
              ▼
┌────────────────────────────────────────────┐
│      Row Level Security (RLS)              │
│  • Lectura pública: proyectos publicados  │
│  • Escritura: solo autenticados (futuro)  │
└────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

📊 BASE DE DATOS (PostgreSQL via Supabase):

categories                projects ────────┐
   ↓                         ↓             │
   └──────> many-to-many <──┘              │
              ↓                             │
       project_technologies                 │
              ↓                             │
         technologies                       │
                                            │
contact_messages ◄──────────────────────────┘

Tablas:
• categories (4 categorías seed)
• technologies (8 tecnologías seed)
• projects (vacía, lista para datos)
• project_technologies (relaciones)
• contact_messages (mensajes de contacto)

Vistas:
• projects_full (JOIN con categorías y tecnologías)

═══════════════════════════════════════════════════════════════

🎯 PRINCIPIOS DE DISEÑO:

✓ Constructivismo (Rodchenko, Lissitzky)
✓ Estética periódico vintage
✓ Colores audaces (rojo + crema + negro)
✓ Tipografía expresiva
✓ Grid riguroso pero dinámico
✓ Textura y grano
✓ Animaciones sutiles

═══════════════════════════════════════════════════════════════

💻 COMANDOS ÚTILES:

# Desarrollo
npm run dev              # Servidor en http://localhost:5173

# Producción
npm run build            # Crear build optimizado
npm run preview          # Preview del build

# Dependencias
npm install              # Instalar paquetes
npm install [package]    # Añadir paquete nuevo

═══════════════════════════════════════════════════════════════

📈 PROGRESO ACTUAL:

[████████████████░░░░░░░░░░░░] 40% Completado

✅ Fase 1: Capa de Datos       (100%)
✅ Fase 2: Sistema de Diseño   (100%)
✅ Fase 3: Homepage            (100%)
⏳ Fase 4: Galería Proyectos   (0%)
⏳ Fase 5: Panel Admin         (0%)
⏳ Fase 6: Contacto            (0%)
⏳ Fase 7: Autenticación       (0%)
⏳ Fase 8: Deploy              (0%)

═══════════════════════════════════════════════════════════════

🚀 LISTO PARA USAR:

1. Configura Supabase (5 min) → Ver SETUP_SUPABASE.md
2. Ejecuta `npm run dev`
3. Abre http://localhost:5173
4. ¡Disfruta tu portafolio constructivista!

═══════════════════════════════════════════════════════════════
